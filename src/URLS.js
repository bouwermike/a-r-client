const base = 'http://localhost:3000'

const URLS = {
    base : base,
    assets : base + '/assets',
    register : base + '/register',
    signin : base + '/signin',
    verifyJWT : base + '/verifyJWT',
    search : base + '/search'
}


export default { URLS }   